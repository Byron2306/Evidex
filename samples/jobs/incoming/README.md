Drop job folders here.

Each job folder must contain:
- `intake.yaml`
- `uploads/` directory with the client source files

Example:
`samples/jobs/incoming/acme_grant_q4/`
  - `intake.yaml`
  - `uploads/` (pdf/docx/xlsx/etc)
